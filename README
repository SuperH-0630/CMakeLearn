CMake Learn
===========
该项目是CMake学习项目, 包括普通构建, 超级构建, 动态库构建等等
该项目的主要功能:
* 构建一个动态库和静态库
* 构建一个可执行程序
* 构建一套测试程序
* 使用了超级构建
* 通过find_package寻找python

git仓库:
通过git仓库记录了该学习项目的不断改进
git的详细信息可以参见git-info文档

feat是指添加了新的功能(新的学习内容)
refactor是指没有添加新的功能但修改了文件结构等
fix是指修复了bug
style是指没有添加新的功能但修改了程序结构等
docx是指添加了新文档